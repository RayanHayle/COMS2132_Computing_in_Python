# Homework 0 

The main purpose of this exercise is to practice using Git and Github classroom.
Make sure to commit and push multiple working version of your code. You should include at least one version for each of the numbered items below. 

The file `binary_search.py` contains a simple implementation of binary search on an array. 

1. Add a function `is_sorted` that returns `True` if the array is in sorted order, and `False` otherwise. Call the function from the main section of the code. 
2. Modify the `binary_search` function so that it calculates and prints the total number of comparisons needed before returning.

